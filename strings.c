#include<stdio.h>
//#include<string.h>
int main()
{
    /*char a[30],b[30];
    int len;
    printf("enter the character of a\n");
    scanf("%s",a);
    printf("enter the character of b\n");
    scanf("%s",b);
    printf("string a before concatenation is %s\n",a);
    strcat(a,b);
    printf("string a is  after concatenation is %s\n",a);
    len=strlen(a);
    printf("the length of string a is %d\n",len);
    printf("reverse of string b is %s\n",strrev(b));
    printf("string before copying is %s\n",a);
    strcpy(a,b);
    printf("the string after copying is %s\n",a);*/
     const char name[10]="sachidanan";
    //printf("enter the the string\n");
    scanf("%s",name);
    printf("%s",name);







}
